@model ProductListViewModel
<h1>
    @Model.CurrentCategory

</h1>
@foreach (var product in Model.Products)
{
    <div>
        <h1>@product.Name</h1>
        <h2>@product.Price.ToString("c")</h2> @* format waluty *@
        <h2>@product.Category.Name</h2>
    </div>
}